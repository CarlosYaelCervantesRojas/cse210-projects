using System;

// "negative goals" where they lose points for bad habits.
class Program
{
    static void Main(string[] args)
    {
        GoalManager goalManager = new GoalManager();

        goalManager.Start();
    }
}